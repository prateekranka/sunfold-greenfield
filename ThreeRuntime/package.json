{
  "name": "sunfold-three-runtime",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:labs": "node scripts/build-labs.mjs",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "three": "^0.178.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
